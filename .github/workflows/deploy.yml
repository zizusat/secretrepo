name: Deployment
on:
  push:
    branches:
      - main
      - dev
env:
  MONGODB_DB_NAME: gha-demo
  #MONGODB_USER_NAME: zizusat
  #MONGODB_PASSWORD: ND2UMi8jklEq9uKv
  
jobs:
  test:
    env:
      MONGODB_CLUSTER_ADDRESS: cluster1.jiw4o60.mongodb.net
      MONGODB_USERNAME: zizusat
      MONGODB_PASSWORD: EzdbKkSodQ69P4Gk
      PORT: 8080
   
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:$PORT
      - name: Run tests
        run: npm test
      - name: Output information
        run: echo "Mongdbusername:${{env.MONGODB_USER_NAME}}"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "..djd."
